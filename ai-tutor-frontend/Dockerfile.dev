# Dockerfile.dev — Frontend dev mode (Vite HMR hot-reload)
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

# Vite dev server lắng nghe trên 0.0.0.0 để Docker expose ra host
CMD ["npx", "vite", "--host", "0.0.0.0", "--port", "3000"]
